@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
        }

        .hero {
            position: relative;
            text-align: center;
            padding: 90px 20px;
            height: 350px; /* Adjust height as needed */
            overflow: hidden;
        }

        .slideshow-container {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 1; /* Behind content */
        }

        .slide {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-size: cover;
            background-position: center center;
            opacity: 0;
            transition: opacity 1s ease-in-out; /* Fade effect for swipe transition */
        }

            .slide.active {
                opacity: 1;
            }

        .hero h2 {
            color: #fff;
            font-size: 2em;
            margin-left: 70px;
            margin-bottom: 50px;
            position: relative;
            z-index: 2; /* Above slideshow */
        }

        .hero button {
            padding: 10px 20px;
            background: #007BFF;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            position: relative;
            z-index: 2; /* Above slideshow */
        }

        /* Navigation dots */
        .dots-container {
            position: absolute;
            bottom: 20px;
            width: 100%;
            text-align: center;
            z-index: 2; /* Above slideshow */
        }

        .dot {
            height: 10px;
            width: 10px;
            margin: 0 5px;
            background-color: #bbb;
            border-radius: 50%;
            display: inline-block;
            cursor: pointer;
            transition: background-color 0.3s;
        }

            .dot.active {
                background-color: #717171;
            }

        /* Navigation arrows */
        .prev, .next {
            position: absolute;
            top: 50%;
            transform: translateY(-50%);
            font-size: 30px;
            color: white;
            background-color: rgba(0, 0, 0, 0.5);
            padding: 10px;
            cursor: pointer;
            user-select: none;
            border-radius: 3px;
            z-index: 2; /* Above slideshow */
        }

        .prev {
            left: 10px;
        }

        .next {
            right: 10px;
        }

            .prev:hover, .next:hover {
                background-color: rgba(0, 0, 0, 0.8);
            }

        .services {
            display: flex;
            justify-content: space-around;
            padding: 40px 20px;
            background: #fff;
        }

        .service {
            text-align: center;
            width: 30%;
        }

            .service h3 {
                color: #007BFF;
            }
    </style>
</head>
<body>
    <header>
    </header>
    <section class="hero">
        <div class="slideshow-container">
            <div class="slide" style="background-image: url('/dist/assets/img/bglaptop.png');"></div>
            <div class="slide" style="background-image: url('/dist/assets/img/bg1.jpeg');"></div>
            <div class="slide" style="background-image: url('/dist/assets/img/bg2.jpeg');"></div>
            <div class="slide" style="background-image: url('/dist/assets/img/bg4.jpeg');"></div>
        </div>
        <div class="dots-container">
            <span class="dot" onclick="currentSlide(0)"></span>
            <span class="dot" onclick="currentSlide(1)"></span>
            <span class="dot" onclick="currentSlide(2)"></span>
            <span class="dot" onclick="currentSlide(3)"></span>
        </div>
        <a class="prev" onclick="changeSlide(-1)">❮</a>
        <a class="next" onclick="changeSlide(1)">❯</a>
    </section>
    <section class="services">
        <div class="service">
            <h3>Simplify</h3>
            <p>Quy trình quản lý hợp đồng trở nên dễ dàng và nhanh chóng.</p>
        </div>
        <div class="service">
            <h3>Amplify</h3>
            <p>Nâng cao hiệu suất và chất lượng công việc của bạn.</p>
        </div>
        <div class="service">
            <h3>Succeed</h3>
            <p>Giúp bạn đạt được thành công lâu dài trong quản lý hợp đồng.</p>
        </div>
    </section>

    <script>
        let slideIndex = 0;
        let slides = document.getElementsByClassName("slide");
        let dots = document.getElementsByClassName("dot");

        function showSlides() {
            // Hide all slides and dots
            for (let i = 0; i < slides.length; i++) {
                slides[i].classList.remove("active");
                dots[i].classList.remove("active");
            }
            // Increment index and loop back if needed
            slideIndex = (slideIndex + 1) % slides.length;
            // Show current slide and dot
            slides[slideIndex].classList.add("active");
            dots[slideIndex].classList.add("active");
        }

        function changeSlide(n) {
            slideIndex = (slideIndex + n + slides.length) % slides.length;
            showSlides();
            resetTimer();
        }

        function currentSlide(n) {
            slideIndex = n;
            showSlides();
            resetTimer();
        }

        // Auto slide every 5 seconds
        let slideInterval = setInterval(showSlides, 5000);

        function resetTimer() {
            clearInterval(slideInterval);
            slideInterval = setInterval(showSlides, 5000);
        }

        // Initialize first slide
        showSlides();
    </script>
</body>
</html>