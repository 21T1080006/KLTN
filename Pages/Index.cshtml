@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
    Layout = "_Layout";
}

<div class="parent">
    <div class="div1">
        <div class="slideshow-container">
            <div class="slide" style="background-image: url('/dist/assets/img/bglaptop.png');">
                <div class="slide-content">
                    <h2>An toàn</h2>
                    <p class="label">Hợp đồng thông minh, vận hành tối ưu</p>
                    <p>Quản lý hợp đồng trực tuyến với công nghệ thông minh, giúp tối ưu hóa quy trình và nâng cao hiệu quả.</p>
                    <div class="dots"></div>
                </div>
            </div>
            <div class="slide" style="background-image: url('/dist/assets/img/bg1.jpeg');">
                <div class="slide-content">
                    <h2>Hiệu quả</h2>
                    <p class="label">Ký nhanh, quản gọn, hiệu quả vượt trội</p>
                    <p> Ký kết hợp đồng tức thì, quản lý thông minh, nâng cao năng suất với nền tảng trực tuyến hiện đại.</p>
                    <div class="dots"></div>
                </div>
            </div>
            <div class="slide" style="background-image: url('/dist/assets/img/bg2.jpeg');">
                <div class="slide-content">
                    <h2>Succeed</h2>
                    <p class="label">Tất cả hợp đồng, một nơi lưu trữ</p>
                    <p>Tập trung quản lý mọi hợp đồng trên một nền tảng số an toàn, dễ dàng truy cập bất cứ lúc nào.</p>
                    <div class="dots"></div>
                </div>
            </div>
            <div class="slide" style="background-image: url('/dist/assets/img/bg4.jpeg');">
                <div class="slide-content">
                    <h2>Easy</h2>
                    <p class="label">Đơn giản hóa hợp đồng, kết nối tương lai</p>
                    <p>Biến việc quản lý hợp đồng trở nên dễ dàng, nhanh chóng,phát triển bền vững cho doanh nghiệp.</p>
                    <div class="dots"></div>
                </div>
            </div>
            <button class="prev">←</button>
            <button class="next">→</button>
        </div>
    </div>
    <div class="div2">
        <h3>Simplify</h3>
        <p>Quy trình quản lý hợp đồng trở nên dễ dàng và nhanh chóng.</p>
    </div>
    <div class="div3">
        <h3>Amplify</h3>
        <p>Nâng cao hiệu suất và chất lượng công việc của bạn.</p>
    </div>
    <div class="div4">
        <h3>Succeed</h3>
        <p>Giúp bạn đạt được thành công lâu dài trong quản lý hợp đồng.</p>
    </div>
</div>

@section Scripts {
    <script>
        let currentSlide = 0;
        const slides = document.querySelectorAll('.slide');
        const totalSlides = slides.length;
        const prevBtn = document.querySelector('.prev');
        const nextBtn = document.querySelector('.next');

        function showSlide(index) {
            slides.forEach((slide, i) => {
                slide.classList.remove('active');
                if (i === index) {
                    slide.classList.add('active');
                }
            });
            updateDots();
        }

        function updateDots() {
            slides.forEach((slide, i) => {
                const dots = slide.querySelector('.dots');
                if (dots) {
                    dots.innerHTML = '';
                    for (let j = 0; j < totalSlides; j++) {
                        const dot = document.createElement('span');
                        if (j === currentSlide) dot.classList.add('active-dot');
                        dots.appendChild(dot);
                    }
                }
            });
        }

        function nextSlide() {
            currentSlide = (currentSlide + 1) % totalSlides;
            showSlide(currentSlide);
        }

        function prevSlide() {
            currentSlide = (currentSlide - 1 + totalSlides) % totalSlides;
            showSlide(currentSlide);
        }

        prevBtn.addEventListener('click', prevSlide);
        nextBtn.addEventListener('click', nextSlide);

        showSlide(currentSlide);

        setInterval(nextSlide, 3000);
    </script>
}

<style>
    .parent {
        display: grid;
        grid-template-columns: repeat(6, 1fr);
        grid-template-rows: repeat(6, 1fr);
        grid-column-gap: 0px;
        grid-row-gap: 0px;
    }

    .div1 {
        grid-area: 1 / 1 / 4 / 7;
        position: relative;
        text-align: center;
        padding: 20px 20px;
        height: 400px;
        overflow: hidden;
    }

    .slideshow-container {
        position: relative;
        width: 100%;
        height: 100%;
    }

    .slide {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        opacity: 0;
        transition: opacity 1s ease-in-out;
        display: flex;
        align-items: center;
        justify-content: center;
        background-size: cover;
        background-position: center;
    }

        .slide.active {
            opacity: 1;
        }

    .slide-content {
        color: #fff;
        text-shadow: 1px 1px 5px rgba(0, 0, 0, 0.7);
        text-align: center;
    }

        .slide-content h2 {
            font-size: 2.5em;
            margin-bottom: 10px;
        }

        .slide-content p {
            font-size: 1.2em;
            margin-bottom: 5px;
        }

        .slide-content .label {
            font-size: 1.7em;
            color: blue;
        }

        .slide-content .dots {
            margin-top: 10px;
        }

    .dots span {
        display: inline-block;
        width: 10px;
        height: 10px;
        background: #ccc;
        border-radius: 50%;
        margin: 0 5px;
    }

    .dots .active-dot {
        background: white;
    }

    .prev, .next {
        position: absolute;
        top: 50%;
        transform: translateY(-50%);
        font-size: 2em;
        color: white;
        background: rgba(0, 0, 0, 0.5);
        border: none;
        padding: 10px;
        cursor: pointer;
        z-index: 10;
    }

    .prev {
        left: 10px;
    }

    .next {
        right: 10px;
    }

    .div2 {
        grid-area: 4 / 1 / 7 / 3;
        padding: 20px;
        color: cornflowerblue;
    }

    .div3 {
        grid-area: 4 / 3 / 7 / 5;
        padding: 20px;
        color: cornflowerblue;
    }

    .div4 {
        grid-area: 4 / 5 / 7 / 7;
        padding: 20px;
        color: cornflowerblue;
    }
</style>